import 'package:flutter/material.dart';

class CompleteTodos extends StatelessWidget {
  const CompleteTodos({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text('Complete Todos'),);
  }
}